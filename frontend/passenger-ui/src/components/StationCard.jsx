import React from 'react';
import { AlertTriangle, Clock3, MapPin } from 'lucide-react';
import DelayChip from './DelayChip';
const time = (value) => new Intl.DateTimeFormat('en-IN', { hour: '2-digit', minute: '2-digit', hour12: false, timeZone: 'Asia/Kolkata' }).format(new Date(value));
const StationCard = ({ station, isNext }) => { const historical = station.prediction_mode === 'trained_historical'; const delayed = station.delay_minutes > 10; return <article className={`station-card ${isNext ? 'station-card-next' : ''}`}><div className="station-rail"><span /></div><div className="flex flex-col gap-5 p-5 sm:flex-row sm:items-start sm:justify-between sm:p-6"><div className="min-w-0"><div className="flex items-center gap-2"><MapPin className="h-4 w-4 text-violet-300" /><span className="text-[10px] font-semibold uppercase tracking-[0.16em] text-slate-500">{historical ? 'Dataset destination' : isNext ? 'Next stop' : station.station_code}</span></div><h3 className="mt-2 text-xl font-semibold tracking-tight text-white">{station.station_name}</h3><div className="mt-4 flex items-center gap-3 text-sm"><span className="text-slate-500">{historical ? 'Original schedule' : 'Demo schedule'} <time className="ml-1 font-mono text-slate-400">{historical ? new Date(station.scheduled_arrival).toLocaleString('en-IN', { timeZone: 'Asia/Kolkata', dateStyle: 'medium', timeStyle: 'short' }) : time(station.scheduled_arrival)}</time></span>{station.platform && <span className="platform-badge">Platform {station.platform}</span>}</div></div><div className="sm:text-right"><p className="text-[10px] font-semibold uppercase tracking-[0.16em] text-slate-500">{historical ? 'Model-predicted historical arrival' : 'Mock expected arrival'}</p><time className={`mt-1 block font-mono text-4xl font-semibold tracking-[-0.05em] ${delayed ? 'text-amber-200' : 'text-emerald-200'}`}>{time(station.predicted_arrival)}</time>{historical && <p className="mt-1 text-xs text-slate-400">{new Date(station.predicted_arrival).toLocaleDateString('en-IN', { timeZone: 'Asia/Kolkata', dateStyle: 'medium' })} · IST</p>}<p className={`mt-1 text-xs ${delayed ? 'text-amber-300/70' : 'text-emerald-300/70'}`}>{station.delay_minutes > 0 ? `${Math.round(station.delay_minutes)} min behind schedule` : 'On schedule'}</p></div></div><div className="mx-5 border-t border-[#332d3c] sm:mx-6" /><div className="grid gap-4 p-5 sm:grid-cols-[1fr_auto] sm:items-center sm:p-6"><div className="confidence-window"><Clock3 className="h-4 w-4 text-violet-300" /><span>{historical ? 'TRAIN-calibrated window' : 'Likely between'} <strong>{historical ? new Date(station.confidence_lower).toLocaleString('en-IN', { timeZone: 'Asia/Kolkata', dateStyle: 'medium', timeStyle: 'short' }) : time(station.confidence_lower)}</strong> and <strong>{historical ? new Date(station.confidence_upper).toLocaleString('en-IN', { timeZone: 'Asia/Kolkata', dateStyle: 'medium', timeStyle: 'short' }) : time(station.confidence_upper)}</strong></span></div>{station.loop_prediction?.probability > 0.6 && <div className="flex items-center gap-2 text-xs text-amber-200"><AlertTriangle className="h-4 w-4" /> Possible hold ahead</div>}</div>{station.delay_reasons?.length > 0 && <div className="grid gap-2 border-t border-[#332d3c] bg-[#211c28] p-5 sm:grid-cols-2 sm:p-6">{station.delay_reasons.map((reason, idx) => <DelayChip key={idx} reason={reason} compact />)}</div>}</article>; };
export default StationCard;
