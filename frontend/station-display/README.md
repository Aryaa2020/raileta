# RailETA Station Display

IPIS-style departure board for station staff.

The board uses the shared departures API, shows scheduled and predicted departures with platforms and delays, and supports keyboard station switching (`1` Chennai Central, `2` Katpadi, `3` KSR Bengaluru, `R` refresh).
